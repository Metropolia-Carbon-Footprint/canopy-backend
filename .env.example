# Local PostgreSQL development configuration
POSTGRES_DB=canopy-db
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_PORT=5432

# Optional: configure a stable JWT signing secret for shared environments.
# Leave unset for local development so the backend generates a temporary key at startup.
# SECURITY_JWT_SECRET=replace-with-a-secure-secret
